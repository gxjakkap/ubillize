{
  "name": "ubillize",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --framework-inference=false --env-mode=loose",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:check": "turbo run db:check",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate"
  },
  "keywords": [],
  "author": "",
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "dotenv": "^16.4.5",
    "turbo": "^2.3.3"
  },
  "dependencies": {
    "drizzle-kit": "^0.28.1"
  }
}
